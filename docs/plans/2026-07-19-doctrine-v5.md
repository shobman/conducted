# Doctrine v5 — external field fixes (compact plan of record)

Evidence source: an external contributor's port review (the external contributor's addendum, v2, emailed 2026-07-19 — conducted's first
outside contribution; their fleet is LAN-locked so the field-note channel ran over email). Owner
ruled: land the fixes, credit the review, PR authored by the bot for owner review.

| # | Change | File(s) | Verification |
|---|---|---|---|
| 1 | Glob semantics: `*` = one segment, `**` = recursive (old `case` matcher let `*` cross `/`) | `seed/scripts/manifest-check.sh` | 13-case matrix, both directions, incl. regex-metachar filenames — all pass pre-commit |
| 2 | Gates workflow script path resolved at source (placeholder deleted) | `seed/.github/workflows/gates.yml` | grep shows `bash scripts/manifest-check.sh` |
| 3 | `.gitattributes` with `*.sh text eol=lf` + copy-list updates | `seed/.gitattributes`, `skills/principal` genesis step 1 + adoption step 3 | file present; skill text names it |
| 4 | Self-contained law: private spec-of-record pointer demoted | `skills/principal/SKILL.md`, `seed/CLAUDE.md` | grep: no "Memento lore" in seed/ or skills/ |
| 5 | Pinned-inputs adoption precondition | `seed/CLAUDE.md` | present under Adoption rule |
| 6 | Version line v5 + MIGRATIONS entry (same PR, contributor rule) | `seed/CLAUDE.md`, `docs/MIGRATIONS.md` | version line reads v5 |

Out of scope, deliberately: multi-repo portfolio nodes (awaiting the external contributor's multi-repo pilot's field
report — evidence-gated); aidos/conducted layering (parked); the aidos bug fixes (separate repo,
separate verify-first PR).
