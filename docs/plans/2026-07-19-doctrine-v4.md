# Doctrine v4 Implementation Plan — the identity harvest

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land doctrine v4 — the identity law (owner identity carries only rulings + approvals; all agent speech bot-authored; hard-stop auth preflight; PAT-expiry forecasting) plus the session-end verified-not-asserted clause — as one version bump with its MIGRATIONS entry.

**Architecture:** First real harvest of the field-notes channel. Receipts: the game project's `docs/field-notes/2026-07-19-session-end-asserted-not-verified.md` @ c31af5c (two receipts: 18 unpushed commits; worktree mounted across two sessions); the queue app's conductor auth stumble (no mechanics existed for "via the bot identity" — session transcript 2026-07-19, field note being filed); the booking app's PR (agent comments posted under the owner's identity — "for the owner" ventriloquism). Owner rulings this session: identity law total (Principal and conductor both bot-authed; owner identity unreachable from agent sessions), hard stop on expired/missing/wrong auth, expiry recorded and forecast. Deferred still: autonomy items (unruled).

**Tech Stack:** Markdown prose + MIGRATIONS entry. Verification = greps.

## Global Constraints

- Version line becomes exactly: `Doctrine version: **v4** (2026-07-19)`
- MIGRATIONS v4 entry in the SAME PR (contributor rule).
- Preflight is a HARD STOP — no wording that permits fallback to owner credentials.
- Branch `v4-identity`; commit per task; PR to main; owner merges.

---

### Task 1: seed/CLAUDE.md — identity law + verified session end

- [ ] Version line: `**v3** (2026-07-19)` → `**v4** (2026-07-19)`.
- [ ] In `## Roles`, after the observation-rule bullet, add:

```markdown
- **Identity law:** the owner's identity carries exactly two things — rulings and approvals,
  clicked or typed by the human, never by a session. Every agent session (Principal and conductor
  alike) authenticates to GitHub as the BOT identity; all agent speech — commits, branches, PRs,
  comments — is bot-authored. Agents may QUOTE the owner (with a DECISIONS pointer); they never
  speak as the owner. **Auth preflight (hard stop):** before any GitHub write, verify
  `gh api user --jq .login` returns the bot. Expired PAT, missing token, or any other login is a
  FULL STOP — surface "bot auth needs renewal (docs/BOT.md)" to the owner and touch nothing on
  GitHub until fixed. Falling back to the owner's credentials is forbidden: a push as the owner
  is worse than no push. Plumbing and the PAT expiry date live in `docs/BOT.md`; wake forecasts
  renewal from it.
```

- [ ] Replace Session end items 1–3 with the verified forms:

```markdown
1. Nothing stranded — verified, not asserted: `git status --porcelain` is empty or every line is
   accounted for in the handoff; every worktree is merged, PR'd, or abandoned with a written reason.
2. Durable knowledge committed AND PUSHED — committed alone is not durable: `git ls-remote --heads
   origin <branch>` resolves for every branch being kept; quote the output in the handoff.
3. Environment restored — `git worktree list` output matches the expected set (main checkout plus
   live nodes only); no stray processes (kill by PID, never by pattern).
```

- [ ] Verify: `grep -c "Identity law" seed/CLAUDE.md` → 1; `grep -c "ls-remote" seed/CLAUDE.md` → ≥1. Commit.

### Task 2: seed/docs/BOT.md — the plumbing record

- [ ] Create with:

```markdown
# Bot identity — plumbing record (owner-maintained)

bot-login: <BOT-USERNAME>
pat-expires: <YYYY-MM-DD>   # wake warns when <14 days out; renewal is an owner task
pat-scope: <THIS-REPO-ONLY — Contents: rw, Pull requests: rw>
last-verified: <YYYY-MM-DD>

## How agent sessions authenticate
Launch agent sessions with the bot's fine-grained PAT in the environment: `GH_TOKEN=<token>` (gh
gives an env token precedence over the keyring) plus `gh auth setup-git` so git pushes use it too.
The owner's keyring stays out of agent reach; approvals happen in the owner's browser. Never paste
the token into an agent conversation — transcripts are not a secrets store.

## Renewal (owner, ~5 min, every PAT cycle)
1. On the bot account: regenerate the fine-grained PAT; re-select THIS repo in the token's
   repository list (the step everyone forgets — collaborator status alone is not enough).
2. Update the launch environment with the new token; update `pat-expires:` above.
3. Any session that hard-stopped on auth resumes normally — nothing else to fix.
```

- [ ] Commit.

### Task 3: skills/conduct — preflight in Bootstrap

- [ ] In §0 Bootstrap, after the worktree-creation sentence ("Work only in your worktree."), add:

```markdown
Auth preflight before any push, PR, or comment: `gh api user --jq .login` must return the BOT
identity (doctrine Identity law). Anything else — expired PAT, missing token, the owner's login —
is a HARD STOP: surface "bot auth needs renewal (docs/BOT.md)" and touch nothing on GitHub until
the owner fixes it. Never fall back to the owner's credentials.
```

- [ ] Commit.

### Task 4: skills/principal — preflight, merge-gate wake check, setup pointers

- [ ] In the cardinal-rules block, add: `- You are an agent: the Identity law binds you exactly as it binds conductors — bot-authed GitHub writes, auth preflight before the first one, hard stop on failure.`
- [ ] In Wake step 2, after the consolidation trigger, add:

```markdown
   Merge-gate check: verify the gate is REAL — bot is a collaborator with write, main requires
   1 approval, recent node PRs are bot-authored, and `docs/BOT.md` pat-expires is >14 days out
   (expiring → renewal on HELD FOR OWNER). A ceremonial gate (agent PRs authored by the owner, or
   0 required approvals) goes on the board as a fix BEFORE any dispatch.
```

- [ ] Genesis step 5: replace "the GitHub setup reminder from the conducted README (bot identity for PR approval) if not yet configured" with "the GitHub setup reminder: run `ops/setup-project-repo.ps1 -Repo <owner/name>` from the conducted repo (bot collaborator + 1-approval merge gate), then fill in `docs/BOT.md`."
- [ ] Adoption step 3 copy list: extend "templates, `scripts/manifest-check.sh` + `scripts/rollup.sh`, the gates workflow" to also name `docs/CONSOLIDATION.md` + `docs/BOT.md` (v3 omitted CONSOLIDATION.md from the adoption list; migrate's ensure-steps covered it, adoption should too).
- [ ] Commit.

### Task 5: MIGRATIONS v4 + STATUS + consistency

- [ ] Append v4 entry (what/why/ritual/conflicts — full text in implementation; why cites the three repos' receipts; ritual is idempotent ensure-style: identity-law block present · BOT.md exists with real values · both skills carry the preflight · wake carries the merge-gate check · session-end items are the verified forms · `ops/setup-project-repo.ps1` run once per repo · existing PRs/comments authored as the owner are HISTORY — never rewritten, but any owner-attributed decision not in DECISIONS gets landed there with honest attribution).
- [ ] Conflicts: repos where the owner intends to keep acting as author (no bot) must rule that explicitly — the merge gate is then ceremonial and wake must say so on every board · the booking app's develop-first promotion model applies the identity law on develop.
- [ ] STATUS line. Greps: `grep -rn "BOT.md" seed/ skills/ | wc -l` ≥ 4; `grep -c "v4" seed/CLAUDE.md` ≥ 1. Commit.

### Task 6: PR

- [ ] Push `v4-identity`; PR titled "feat(v4): identity law — the owner's identity carries only rulings and approvals"; decisions-first body citing the three receipts; owner merges.

## Self-review notes
- All four queue items covered: identity law (T1), session-end verification (T1), bot mechanics + wake check (T2/T3/T4), setup-script integration (T4). Stopping-rule rationale is cite-only (no law change) — noted in MIGRATIONS why. Autonomy: deferred, unruled.
- Placeholder scan: BOT.md placeholders are template fields (legal; lives in docs/, outside `work/**` gate scope).
