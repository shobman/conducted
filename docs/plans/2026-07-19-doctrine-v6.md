# Doctrine v6 — gate integrity (compact plan of record)

Evidence: the queue app's field report 2026-07-19 (work/server-discovery) — third instance in one week of
"a gate rejecting what doctrine mandates" (v5's field-notes/manifest fix was the first; a second hit
deltas-vs-trailerless; the third hit generics-vs-placeholder). Scope: contradicts-class only,
per the product-first freeze's own exception. Everything else in the queue stays queued.

| # | Change | File(s) | Verification |
|---|---|---|---|
| 1 | Law: gates never reject mandated artifacts + measured-party-never-edits-measurers corollary | `seed/CLAUDE.md` | text present; version v6 |
| 2 | Placeholder scan scoped to template-derived files; regex `<[A-Z][A-Z0-9_-]+>`; ± mojibake gone | `seed/.github/workflows/gates.yml` | 14-case fixture matrix, both directions, all pass |
| 3 | Trailerless set gains root CLAUDE.md + `work/*/brief.md` (widening deltas land on base) | gates.yml | pathspec excludes verified |
| 4 | Bot-mode commit-authorship gate (mode + login from BOT.md on BASE ref) | gates.yml | template-placeholder mode string safely skips |
| 5 | Wrapper sets GIT_AUTHOR/COMMITTER env for bot-mode repos (root cause of the 5 mis-authored merges) | `ops/conduct-wrapper.ps1` | PS 5.1 parse OK |
| 6 | ops/*.ps1 ASCII-only (PS 5.1 cp1252 parse death) | all three ops scripts | PS 5.1 tokenizer passes all |
| 7 | Conducted self-check CI: gate fixtures both directions, glob fixtures, ops parse+ASCII | `.github/workflows/selfcheck.yml` | runs on this very PR |
| 8 | MIGRATIONS v6 entry, same PR | `docs/MIGRATIONS.md` | present |

Deliberately NOT in v6 (freeze holds): silent-completion law · scribe graduation · autonomy items ·
multi-repo extension · non-docs migration landing path (partially eased by #3; full answer waits
for evidence).
